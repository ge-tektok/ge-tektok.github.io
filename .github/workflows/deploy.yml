# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  # mainブランチにプッシュされた時に実行します
  push:
    branches: ["main"]
  # 手動でアクションを実行できるようにします
  workflow_dispatch:

# GITHUB_TOKENの権限を設定
permissions:
  contents: read
  pages: write
  id-token: write

# 同時実行を制御（古いデプロイをキャンセル）
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24 # お使いのNodeバージョンに合わせます
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci # package-lock.jsonを使ってクリーンインストール
        
      - name: Build
        run: npm run build
        
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Viteのデフォルトのビルド出力先を指定
          path: './dist'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4